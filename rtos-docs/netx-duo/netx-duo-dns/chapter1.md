---
title: Bölüm 1-Azure RTOS NetX Duo DNS Istemcisine giriş
description: DNS, etki alanı adlarıyla fiziksel IP adresleri arasında eşleme içeren dağıtılmış bir veritabanı sağlar.
author: philmea
ms.author: philmea
ms.date: 06/04/2020
ms.topic: article
ms.service: rtos
ms.openlocfilehash: 4c07d6e3183d421c637874dcdeff3767554fca78
ms.sourcegitcommit: e3d42e1f2920ec9cb002634b542bc20754f9544e
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 03/22/2021
ms.locfileid: "104826026"
---
# <a name="chapter-1---introduction-to-the-azure-rtos-netx-duo-dns-client"></a><span data-ttu-id="fd658-103">Bölüm 1-Azure RTOS NetX Duo DNS Istemcisine giriş</span><span class="sxs-lookup"><span data-stu-id="fd658-103">Chapter 1 - Introduction to the Azure RTOS NetX Duo DNS Client</span></span>

<span data-ttu-id="fd658-104">DNS, etki alanı adlarıyla fiziksel IP adresleri arasında eşleme içeren dağıtılmış bir veritabanı sağlar.</span><span class="sxs-lookup"><span data-stu-id="fd658-104">The DNS provides a distributed database that contains mapping between domain names and physical IP addresses.</span></span> <span data-ttu-id="fd658-105">Tüm eşlemeyi içeren Internet 'te tek bir varlık bulunmadığından, veritabanı *dağıtıldı* olarak adlandırılır.</span><span class="sxs-lookup"><span data-stu-id="fd658-105">The database is referred to as *distributed* because there is no single entity on the Internet that contains the complete mapping.</span></span> <span data-ttu-id="fd658-106">Eşlemenin bir kısmını tutan bir varlığa, DNS sunucusu denir.</span><span class="sxs-lookup"><span data-stu-id="fd658-106">An entity that maintains a portion of the mapping is called a DNS Server.</span></span> <span data-ttu-id="fd658-107">Internet, her biri bir veritabanının alt kümesini içeren çok sayıda DNS sunucusundan oluşur.</span><span class="sxs-lookup"><span data-stu-id="fd658-107">The Internet is composed of numerous DNS Servers, each of which contains a subset of the database.</span></span> <span data-ttu-id="fd658-108">DNS sunucuları, etki alanı adı eşleme bilgileri için DNS Istemci isteklerini de yalnızca sunucuda istenen eşleme varsa yanıtlar.</span><span class="sxs-lookup"><span data-stu-id="fd658-108">DNS Servers also respond to DNS Client requests for domain name mapping information, only if the server has the requested mapping.</span></span>

<span data-ttu-id="fd658-109">NetX Duo için DNS Istemci protokolü, uygulamayı bir veya daha fazla DNS sunucusundan eşleme bilgilerini istemek üzere hizmetlerle birlikte sağlar.</span><span class="sxs-lookup"><span data-stu-id="fd658-109">The DNS Client protocol for NetX Duo provides the application with services to request mapping information from one or more DNS Servers.</span></span>

## <a name="dns-client-setup"></a><span data-ttu-id="fd658-110">DNS Istemcisi kurulumu</span><span class="sxs-lookup"><span data-stu-id="fd658-110">DNS Client Setup</span></span>

<span data-ttu-id="fd658-111">Düzgün çalışması için, DNS Istemci paketi bir NetX Duo IP örneğinin zaten oluşturulmuş olmasını gerektirir.</span><span class="sxs-lookup"><span data-stu-id="fd658-111">In order to function properly, the DNS Client package requires that a NetX Duo IP instance has already been created.</span></span>

<span data-ttu-id="fd658-112">DNS Istemcisini oluşturduktan sonra, uygulamanın DNS Istemcisi tarafından tutulan sunucu listesine bir veya daha fazla DNS sunucusu eklemesi gerekir.</span><span class="sxs-lookup"><span data-stu-id="fd658-112">After creating the DNS Client, the application must add one or more DNS servers to the server list maintained by the DNS Client.</span></span> <span data-ttu-id="fd658-113">Uygulama, DNS sunucuları eklemek için *nxd_dns_server_add* hizmetini kullanır.</span><span class="sxs-lookup"><span data-stu-id="fd658-113">To add DNS servers, the application uses the *nxd_dns_server_add* service.</span></span> <span data-ttu-id="fd658-114">NetX Duo DNS Istemci hizmeti *nx_dns_server_add* sunucu eklemek için de kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="fd658-114">The NetX Duo DNS Client service *nx_dns_server_add* can also be used to add servers.</span></span> <span data-ttu-id="fd658-115">Ancak yalnızca IPv4 adreslerini kabul eder ve geliştiricilerin bunun yerine *nxd_dns_server_add* hizmetini kullanması önerilir.</span><span class="sxs-lookup"><span data-stu-id="fd658-115">However it only accepts IPv4 addresses and it is recommended that developers use the *nxd_dns_server_add* service instead.</span></span>

<span data-ttu-id="fd658-116">NX_DNS_IP_GATEWAY_SERVER seçeneği etkinse ve IP örneği ağ geçidi adresi sıfır değilse, IP örneği ağ geçidi, birincil DNS sunucusu olarak otomatik olarak eklenir.</span><span class="sxs-lookup"><span data-stu-id="fd658-116">If the NX_DNS_IP_GATEWAY_SERVER option is enabled, and the IP instance gateway address is non zero, the IP instance gateway is automatically added as the primary DNS server.</span></span> <span data-ttu-id="fd658-117">DNS sunucusu bilgileri statik olarak bilinmiyorsa, NetX Duo için dinamik ana bilgisayar Yapılandırma Protokolü (DHCP) üzerinden de türetilebilir.</span><span class="sxs-lookup"><span data-stu-id="fd658-117">If DNS server information is not statically known, it may also be derived through the Dynamic Host Configuration Protocol (DHCP) for NetX Duo.</span></span> <span data-ttu-id="fd658-118">Daha fazla bilgi için lütfen NetX Duo DHCP Kullanıcı kılavuzuna bakın.</span><span class="sxs-lookup"><span data-stu-id="fd658-118">Please refer to the NetX Duo DHCP User Guide for more information.</span></span>

<span data-ttu-id="fd658-119">DNS Istemcisi, DNS iletilerini iletmek için bir paket havuzu gerektirir.</span><span class="sxs-lookup"><span data-stu-id="fd658-119">The DNS Client requires a packet pool for transmitting DNS messages.</span></span> <span data-ttu-id="fd658-120">Varsayılan olarak, *nx_dns_create* HIZMETI çağrıldığında DNS istemcisi bu paket havuzunu oluşturur.</span><span class="sxs-lookup"><span data-stu-id="fd658-120">By default, the DNS Client creates this packet pool when the *nx_dns_create* service is called.</span></span> <span data-ttu-id="fd658-121">Yapılandırma seçenekleri NX_DNS_PACKET_PAYLOAD_UNALIGNED ve NX_DNS_PACKET_POOL_SIZE uygulamanın, bu paket havuzunun paket yükünü ve paket havuzu boyutunu (ör. paket sayısı) belirlemesine izin verir.</span><span class="sxs-lookup"><span data-stu-id="fd658-121">The configuration options NX_DNS_PACKET_PAYLOAD_UNALIGNED and NX_DNS_PACKET_POOL_SIZE allow the application to determine the packet payload and packet pool size (e.g. number of packets) of this packet pool respectively.</span></span> <span data-ttu-id="fd658-122">Bu seçenekler, Bölüm Iki kısmında "yapılandırma seçenekleri" bölümünde açıklanmaktadır.</span><span class="sxs-lookup"><span data-stu-id="fd658-122">These options are described in section “Configuration Options” in Chapter Two.</span></span>

<span data-ttu-id="fd658-123">Kendi paket havuzunu oluşturan DNS Istemcisinin bir alternatifi, uygulamanın paket havuzunu oluşturması ve bu hizmeti *nx_dns_packet_pool_set* HIZMETINI kullanarak DNS istemcisinin paket havuzu olarak ayarlaması içindir.</span><span class="sxs-lookup"><span data-stu-id="fd658-123">An alternative to the DNS Client creating its own packet pool is for the application to create the packet pool and set it as the DNS Client’s packet pool using the *nx_dns_packet_pool_set* service.</span></span> <span data-ttu-id="fd658-124">Bunu yapmak için NX_DNS_CLIENT_USER_CREATE_PACKET_POOL seçeneğinin tanımlanması gerekir.</span><span class="sxs-lookup"><span data-stu-id="fd658-124">To do so, the NX_DNS_CLIENT_USER_CREATE_PACKET_POOL option must be defined.</span></span> <span data-ttu-id="fd658-125">Bu seçenek ayrıca, *nx_dns_packet_pool_set* için paket havuzu işaretçisi girişi olarak *nx_packet_pool_create* kullanarak daha önce oluşturulmuş bir paket havuzu gerektirir.</span><span class="sxs-lookup"><span data-stu-id="fd658-125">This option also requires a previously created packet pool using *nx_packet_pool_create* as the packet pool pointer input to *nx_dns_packet_pool_set* .</span></span> <span data-ttu-id="fd658-126">DNS Istemcisi örneği silindiğinde, artık gerekmiyorsa, uygulama DNS Istemci paket NX_DNS_CLIENT_USER_CREATE_PACKET_POOL havuzunu silmekten sorumludur.</span><span class="sxs-lookup"><span data-stu-id="fd658-126">When the DNS Client instance is deleted, the application is responsible for deleting the DNS Client packet pool if NX_DNS_CLIENT_USER_CREATE_PACKET_POOL is enabled if it is no longer needed.</span></span>

> [!NOTE] 
> <span data-ttu-id="fd658-127">NX_DNS_CLIENT_USER_CREATE_PACKET_POOL seçeneğini kullanarak kendi paket havuzunu sağlamayı tercih eden uygulamalar için, paket boyutunun DNS en yüksek maszu boyutunu (512 bayt) ve UDP üst bilgisi, IPv4 veya IPv6 üst bilgisi ve MAC üst bilgisi için Odalar ' ı tutabilecek olması gerekir.</span><span class="sxs-lookup"><span data-stu-id="fd658-127">For applications choosing to provide its own packet pool using the NX_DNS_CLIENT_USER_CREATE_PACKET_POOL option, the packet size needs to be able to hold the DNS maximum massage size (512 bytes) plus rooms for UDP header, IPv4 or IPv6 header, and the MAC header.</span></span>

## <a name="dns-messages"></a><span data-ttu-id="fd658-128">DNS Iletileri</span><span class="sxs-lookup"><span data-stu-id="fd658-128">DNS Messages</span></span>

<span data-ttu-id="fd658-129">DNS, ana bilgisayar adları ve IP adresleri arasında eşleme almak için çok basit bir mekanizmaya sahiptir.</span><span class="sxs-lookup"><span data-stu-id="fd658-129">The DNS has a very simple mechanism for obtaining mapping between host names and IP addresses.</span></span> <span data-ttu-id="fd658-130">DNS Istemcisi, bir eşleme almak için, ad veya çözümlenmesi gereken IP adresini içeren bir DNS sorgu iletisi hazırlar.</span><span class="sxs-lookup"><span data-stu-id="fd658-130">To obtain a mapping, the DNS Client prepares a DNS query message containing the name or the IP address that needs to be resolved.</span></span> <span data-ttu-id="fd658-131">İleti daha sonra sunucu listesindeki ilk DNS sunucusuna gönderilir.</span><span class="sxs-lookup"><span data-stu-id="fd658-131">The message is then sent to the first DNS server in the server list.</span></span> <span data-ttu-id="fd658-132">Sunucuda böyle bir eşleme varsa, istenen eşleme bilgilerini içeren bir DNS yanıt iletisi kullanarak DNS Istemcisine yanıt verir.</span><span class="sxs-lookup"><span data-stu-id="fd658-132">If the server has such a mapping, it replies to the DNS Client using a DNS response message that contains the requested mapping information.</span></span> <span data-ttu-id="fd658-133">Sunucu yanıt vermezse, DNS Istemcisi, tüm DNS sunucuları sorgulanana kadar listesinde bir sonraki sunucuyu sorgular.</span><span class="sxs-lookup"><span data-stu-id="fd658-133">If the server does not respond, the DNS Client queries the next server on its list until all its DNS servers have been queried.</span></span> <span data-ttu-id="fd658-134">Tüm DNS sunucularından yanıt alınmıyorsa DNS Istemcisinin, DNS iletisini yeniden aktarım mantığı vardır.</span><span class="sxs-lookup"><span data-stu-id="fd658-134">If no response from all its DNS servers is received, the DNS Client has retry logic to retransmit the DNS message.</span></span> <span data-ttu-id="fd658-135">Bir DNS sorgusunu yeniden gönderme sırasında yeniden iletim zaman aşımı iki katına çıkar.</span><span class="sxs-lookup"><span data-stu-id="fd658-135">On resending a DNS query, the retransmission timeout is doubled.</span></span> <span data-ttu-id="fd658-136">Bu işlem, en fazla iletim zaman aşımı ( *nxd_dns. h* içinde NX_DNS_MAX_RETRANS_TIMEOUT olarak tanımlanır) ulaşılıncaya veya bu sunucudan başarılı bir yanıt alınana kadar devam eder.</span><span class="sxs-lookup"><span data-stu-id="fd658-136">This process continues until the maximum transmission timeout (defined as NX_DNS_MAX_RETRANS_TIMEOUT in *nxd_dns.h*) is reached or until a successful response is received from that server is obtained.</span></span>

<span data-ttu-id="fd658-137">NetX Duo DNS Istemcisi, *nxd_dns_host_by_name_get* çağrısında IP adresinin sürümünü belirterek hem IPv6 adres aramalarını (tür aaaa) hem de IPv4 adresi aramalarını (tür A) gerçekleştirebilir.</span><span class="sxs-lookup"><span data-stu-id="fd658-137">NetX Duo DNS Client can perform both IPv6 address lookups (type AAAA) and IPv4 address lookups (type A) by specifying the version of the IP address in the *nxd_dns_host_by_name_get* call.</span></span> <span data-ttu-id="fd658-138">DNS Istemcisi, *nxd_dns_host_by_address_get* kullanarak Web ana bilgisayar adlarını almak için IP ADRESLERININ (PTR sorguları yazın) ters aramalarını gerçekleştirebilir.</span><span class="sxs-lookup"><span data-stu-id="fd658-138">The DNS Client can perform reverse lookups of IP addresses (type PTR queries) to obtain web host names using *nxd_dns_host_by_address_get*.</span></span> <span data-ttu-id="fd658-139">NetX Duo DNS Istemcisi, eşdeğer hizmetler olan ancak IPv4 ağ iletişimi ile sınırlı olan *nx_dns_host_by_name_get* ve *nx_dns_host_by_address_get* desteklemektedir.</span><span class="sxs-lookup"><span data-stu-id="fd658-139">The NetX Duo DNS Client still supports the *nx_dns_host_by_name_get* and *nx_dns_host_by_address_get* which are the equivalent services but which are limited to IPv4 network communication.</span></span> <span data-ttu-id="fd658-140">Ancak, geliştiriciler mevcut DNS Istemci uygulamalarının *nxd_dns_host_by_name_get* ve *nxd_dns_host_by_address_get* hizmetlerine bağlantı altına alınır.</span><span class="sxs-lookup"><span data-stu-id="fd658-140">However, developers are encouraged to port existing DNS Client applications to the *nxd_dns_host_by_name_get* and *nxd_dns_host_by_address_get* services.</span></span>

<span data-ttu-id="fd658-141">DNS mesajlaşma istekleri ve alan yanıtlarını göndermek için UDP protokolünü kullanır.</span><span class="sxs-lookup"><span data-stu-id="fd658-141">DNS messaging utilizes the UDP protocol to send requests and field responses.</span></span> <span data-ttu-id="fd658-142">DNS sunucusu, istemcilerden gelen sorgular için 53 numaralı bağlantı noktasını dinler.</span><span class="sxs-lookup"><span data-stu-id="fd658-142">A DNS Server listens on port number 53 for queries from clients.</span></span> <span data-ttu-id="fd658-143">Bu nedenle, daha önce oluşturulmuş bir IP örneğinde (_ *_nx_ip_create_* \*) \***nx_udp_enable** _ hizmeti kullanılarak NETX Duo 'da UDP Hizmetleri etkinleştirilmelidir.</span><span class="sxs-lookup"><span data-stu-id="fd658-143">Therefore UDP services must be enabled in NetX Duo using the ***nx_udp_enable** _ service on a previously created IP instance (_*_nx_ip_create_\*\*).</span></span>

<span data-ttu-id="fd658-144">Bu noktada, DNS Istemcisi, uygulamadan gelen istekleri kabul etmeye ve DNS sorguları göndermeye hazır hale gelir.</span><span class="sxs-lookup"><span data-stu-id="fd658-144">At this point, the DNS Client is ready to accept requests from the application and send out DNS queries.</span></span>

## <a name="extended-dns-resource-record-types"></a><span data-ttu-id="fd658-145">Genişletilmiş DNS kaynak kaydı türleri</span><span class="sxs-lookup"><span data-stu-id="fd658-145">Extended DNS Resource Record Types</span></span>

<span data-ttu-id="fd658-146">NX_DNS_ENABLE_EXTENDED_RR_TYPES etkinleştirilirse NetX Duo DNS Istemcisi aşağıdaki kayıt türü sorgularını de destekler:</span><span class="sxs-lookup"><span data-stu-id="fd658-146">If NX_DNS_ENABLE_EXTENDED_RR_TYPES is enabled, NetX Duo DNS Client also supports the following record type queries:</span></span>

- <span data-ttu-id="fd658-147">CNAME: bir diğer ad için kurallı adı içerir</span><span class="sxs-lookup"><span data-stu-id="fd658-147">CNAME: contains the canonical name for an alias</span></span>
- <span data-ttu-id="fd658-148">TXT: bir metin dizesi içerir</span><span class="sxs-lookup"><span data-stu-id="fd658-148">TXT: contains a text string</span></span>
- <span data-ttu-id="fd658-149">NS: bir yetkili ad sunucusu içerir</span><span class="sxs-lookup"><span data-stu-id="fd658-149">NS: contains an authoritative name server</span></span>
- <span data-ttu-id="fd658-150">SOA: bir yetki bölgesinin başlangıcını içerir</span><span class="sxs-lookup"><span data-stu-id="fd658-150">SOA: contains the start of a zone of authority</span></span>
- <span data-ttu-id="fd658-151">MX: posta alışverişi için kullanıldı</span><span class="sxs-lookup"><span data-stu-id="fd658-151">MX: used for mail exchange</span></span>
- <span data-ttu-id="fd658-152">SRV: etki alanı tarafından sunulan hizmet hakkındaki bilgileri içerir</span><span class="sxs-lookup"><span data-stu-id="fd658-152">SRV: contains information on the service offered by the domain</span></span>

<span data-ttu-id="fd658-153">CNAME ve TXT kayıt türlerinin dışında, uygulamanın DNS veri kaydını almak için 4 baytlık hizalanmış bir arabellek sağlaması gerekir.</span><span class="sxs-lookup"><span data-stu-id="fd658-153">With the exception of CNAME and TXT record types, the application must supply a 4-byte aligned buffer to receive the DNS data record.</span></span>

<span data-ttu-id="fd658-154">NetX Duo DNS Istemcisinde, kayıt verileri, arabellek alanının en verimli şekilde kullanılmasını sağlamak için bu şekilde depolanır.</span><span class="sxs-lookup"><span data-stu-id="fd658-154">In NetX Duo DNS Client, record data is stored in such a way to make most efficient use of buffer space.</span></span>

<span data-ttu-id="fd658-155">Sabit uzunlukta bir kayıt arabelleği örneği (tür AAAA kaydı) aşağıda gösterilmiştir:</span><span class="sxs-lookup"><span data-stu-id="fd658-155">An example of a record buffer of fixed length (type AAAA record) is shown below:</span></span>

![Sabit uzunluktaki kayıt arabelleğini](media/image2.png)

<span data-ttu-id="fd658-157">Kayıt türleri değişken veri uzunluğuna sahip olan sorgular için, ana bilgisayar adları değişken uzunlukta olan NS kayıtları gibi, NetX Duo DNS Istemcisi verileri aşağıdaki gibi kaydeder.</span><span class="sxs-lookup"><span data-stu-id="fd658-157">For those queries whose record types have variable data length, such as NS records whose host names are of variable length, NetX Duo DNS Client saves the data as follows.</span></span> <span data-ttu-id="fd658-158">DNS Istemcisi sorgusunda sağlanan arabellek, sabit uzunluklu verilerin bir alanı ve yapılandırılmamış bellek alanı halinde düzenlenir.</span><span class="sxs-lookup"><span data-stu-id="fd658-158">The buffer supplied in the DNS Client query is organized into an area of fixed length data and an area of unstructured memory.</span></span> <span data-ttu-id="fd658-159">Bellek arabelleğinin en üstü 4 baytlık hizalanmış kayıt girişlerine göre düzenlenmiştir.</span><span class="sxs-lookup"><span data-stu-id="fd658-159">The top of the memory buffer is organized into 4-byte aligned record entries.</span></span> <span data-ttu-id="fd658-160">Her kayıt girdisi, IP adresini ve bu IP adresi için değişken uzunluklu verilere yönelik bir işaretçi içerir.</span><span class="sxs-lookup"><span data-stu-id="fd658-160">Each record entry contains the IP address and a pointer to the variable length data for that IP address.</span></span> <span data-ttu-id="fd658-161">Her IP adresi için değişken uzunluklu veri, bellek arabelleğinin sonundan itibaren yapılandırılmamış alan belleğinde depolanır.</span><span class="sxs-lookup"><span data-stu-id="fd658-161">The variable length data for each IP address are stored in the unstructured area memory starting at the end of the memory buffer.</span></span> <span data-ttu-id="fd658-162">Art arda her bir kayıt girişinin değişken uzunluğu verileri, önceki kayıt girişleri değişken verilerine bitişik bir sonraki alan belleğine kaydedilir.</span><span class="sxs-lookup"><span data-stu-id="fd658-162">The variable length data for each successive record entry is saved in the next area memory adjacent to the previous record entries variable data.</span></span> <span data-ttu-id="fd658-163">Bu nedenle, değişken veri, başka bir kayıt girişi ve değişken verileri depolamak için yeterli bellek olmadığından, kayıt girdilerini içeren yapılandırılmış belleğin yapısal alanına doğru bir şekilde ' büyür '.</span><span class="sxs-lookup"><span data-stu-id="fd658-163">Hence, the variable data ‘grows’ towards the structured area of memory containing the record entries until there is insufficient memory to store another record entry and variable data.</span></span>

<span data-ttu-id="fd658-164">Bu, aşağıdaki şekilde gösterilmiştir:</span><span class="sxs-lookup"><span data-stu-id="fd658-164">This is shown in the figure below:</span></span>

![Şekil 2](media/image3.png)

<span data-ttu-id="fd658-166">DNS etki alanı adı (NS) veri depolama örneği yukarıda gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="fd658-166">The example of the DNS domain name (NS) data storage is shown above.</span></span>

<span data-ttu-id="fd658-167">NetX Duo DNS Istemci sorguları kayıt depolama biçimini kullanarak, kayıt arabelleğine kaydedilen kayıt sayısını döndürür.</span><span class="sxs-lookup"><span data-stu-id="fd658-167">NetX Duo DNS Client queries using the record storage format return the number of records saved to the record buffer.</span></span> <span data-ttu-id="fd658-168">Bu bilgiler, uygulamanın kayıt arabelleğindeki NS kayıtlarını ayıklamasına olanak sağlar.</span><span class="sxs-lookup"><span data-stu-id="fd658-168">This information enables the application to extract NS records from the record buffer.</span></span>

<span data-ttu-id="fd658-169">Bu kayıt depolama biçimini kullanarak değişken uzunlukta DNS verilerini depolayan DNS Istemci sorgusunun bir örneği aşağıda gösterilmiştir:</span><span class="sxs-lookup"><span data-stu-id="fd658-169">An example of a DNS Client query that stores variable length DNS data using this record storage format is shown below:</span></span>

```C
UINT  _nx_dns_domain_name_server_get(NX_DNS *dns_ptr, 
                    UCHAR *host_name, VOID *record_buffer, 
                    UINT buffer_size, UINT *record_count, 
                    ULONG wait_option);
```


<span data-ttu-id="fd658-170">"DNS Istemci hizmetlerinin açıklaması" başlıklı Bölüm 3 ' te daha fazla ayrıntı bulabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="fd658-170">More details are available in Chapter 3, “Description of DNS Client Services”.</span></span>

## <a name="dns-cache"></a><span data-ttu-id="fd658-171">DNS önbelleği</span><span class="sxs-lookup"><span data-stu-id="fd658-171">DNS Cache</span></span>

<span data-ttu-id="fd658-172">NX_DNS_CACHE_ENABLE etkinleştirilirse NetX Duo DNS Istemcisi DNS önbelleği özelliğini destekler.</span><span class="sxs-lookup"><span data-stu-id="fd658-172">If NX_DNS_CACHE_ENABLE is enabled, NetX Duo DNS Client supports the DNS Cache feature.</span></span> <span data-ttu-id="fd658-173">DNS Istemcisini oluşturduktan sonra uygulama, özel DNS önbelleğini ayarlamak için API *nx_dns_cache_initialize ()* çağırabilir.</span><span class="sxs-lookup"><span data-stu-id="fd658-173">After creating the DNS Client, the application can call the API *nx_dns_cache_initialize()* to set the special DNS Cache.</span></span> <span data-ttu-id="fd658-174">DNS önbelleği özelliğini etkinleştir özelliği, DNS Istemcisi, DNS sorgusu gönderilmeye başlamadan önce DNS önbelleğinden kullanılabilir yanıtı bulacak, kullanılabilir yanıtı bul, doğrudan uygulamaya yanıt döndürür, aksi takdirde DNS Istemcisi DNS sunucusuna sorgu iletisi gönderir ve yanıtı bekler.</span><span class="sxs-lookup"><span data-stu-id="fd658-174">If enable DNS Cache feature, DNS Client will find the available answer from DNS Cache before starts to send DNS query, if find the available answer, directly return the answer to application, otherwise DNS Client sends out query message to DNS server and waits for the reply.</span></span> <span data-ttu-id="fd658-175">DNS Istemcisi yanıt iletisini aldığında ve kullanılabilir boş önbellek varsa, DNS Istemcisi yanıtı uygulamaya döndürür ve aynı zamanda yanıtı kaynak kaydı olarak DNS önbelleğine ekler.</span><span class="sxs-lookup"><span data-stu-id="fd658-175">When DNS Client gets the response message and there is free cache available, DNS Client returns the answer to the application and also adds the answer as resource record into DNS cache.</span></span>

<span data-ttu-id="fd658-176">Her biri önbellekte bir veri yapısını *NX_DNS_RR* (kaynak kaydı).</span><span class="sxs-lookup"><span data-stu-id="fd658-176">Each answer a data structure *NX_DNS_RR* (Resource Record) in the cache.</span></span> <span data-ttu-id="fd658-177">Kayıtlardaki dizeler (kaynak kaydı adı ve verileri) değişken uzunluktadır, bu nedenle NX_DNS_RR yapısında depolanmaz.</span><span class="sxs-lookup"><span data-stu-id="fd658-177">Strings (resource record name and data) in Records are variable length, therefore are not stored in the NX_DNS_RR structure.</span></span> <span data-ttu-id="fd658-178">Kayıt, dizelerin depolandığı gerçek bellek konumunun işaretçilerini içerir.</span><span class="sxs-lookup"><span data-stu-id="fd658-178">The Record contains pointers to the actual memory location where the strings are stored.</span></span> <span data-ttu-id="fd658-179">Dize tablosu ve kayıtlar önbelleği paylaşır.</span><span class="sxs-lookup"><span data-stu-id="fd658-179">The string table and the Records share the cache.</span></span> <span data-ttu-id="fd658-180">Kayıtlar önbelleğin başından itibaren saklanır ve önbelleğin sonuna doğru artar.</span><span class="sxs-lookup"><span data-stu-id="fd658-180">Records are stored from the beginning of the cache, and grow towards the end of the cache.</span></span> <span data-ttu-id="fd658-181">Dize tablosu önbelleğin sonundan başlar ve önbelleğin başına doğru artar.</span><span class="sxs-lookup"><span data-stu-id="fd658-181">The string table starts from the end of the cache and grows towards the beginning of the cache.</span></span> <span data-ttu-id="fd658-182">Dize tablosundaki her bir dizenin bir uzunluk alanı ve bir sayaç alanı vardır.</span><span class="sxs-lookup"><span data-stu-id="fd658-182">Each string in the string table has a length field and a counter field.</span></span> <span data-ttu-id="fd658-183">Dize tablosuna bir dize eklendiğinde, tabloda aynı dize zaten varsa, sayaç değeri artırılır ve dize için bellek ayrılmaz.</span><span class="sxs-lookup"><span data-stu-id="fd658-183">When a string is added to the string table, if the same string is already present in the table, the counter value is incremented and no memory is allocated for the string.</span></span> <span data-ttu-id="fd658-184">Önbellekte daha fazla kaynak kaydı veya yeni dize eklenebileceği takdirde önbellek tam olarak değerlendirilir.</span><span class="sxs-lookup"><span data-stu-id="fd658-184">The cache is considered full if no more resource records or new strings can be added to the cache.</span></span>

## <a name="dns-client-limitations"></a><span data-ttu-id="fd658-185">DNS Istemci sınırlamaları</span><span class="sxs-lookup"><span data-stu-id="fd658-185">DNS Client Limitations</span></span>

<span data-ttu-id="fd658-186">DNS Istemcisi tek seferde bir DNS isteğini destekler.</span><span class="sxs-lookup"><span data-stu-id="fd658-186">The DNS Client supports one DNS request at a time.</span></span> <span data-ttu-id="fd658-187">Önceki DNS isteği tamamlanana kadar başka bir DNS isteği yapmaya çalışan iş parçacıkları geçici olarak engellenir.</span><span class="sxs-lookup"><span data-stu-id="fd658-187">Threads attempting to make another DNS request are temporarily blocked until the previous DNS request is complete.</span></span>

<span data-ttu-id="fd658-188">NetX Duo DNS Istemcisi, ek DNS sorgularını diğer DNS sunucularına iletmek için yetkili yanıtlardan veri kullanmaz.</span><span class="sxs-lookup"><span data-stu-id="fd658-188">The NetX Duo DNS Client does not use data from authoritative answers to forward additional DNS queries to other DNS servers.</span></span>

## <a name="dns-rfcs"></a><span data-ttu-id="fd658-189">DNS RFC 'Leri</span><span class="sxs-lookup"><span data-stu-id="fd658-189">DNS RFCs</span></span>

<span data-ttu-id="fd658-190">NetX Duo DNS, aşağıdaki RFC 'lerle uyumludur:</span><span class="sxs-lookup"><span data-stu-id="fd658-190">NetX Duo DNS is compliant with the following RFCs:</span></span>

- <span data-ttu-id="fd658-191">RFC1034 ETKI ALANı ADLARı-KAVRAMLAR VE TESISLER</span><span class="sxs-lookup"><span data-stu-id="fd658-191">RFC1034 DOMAIN NAMES - CONCEPTS AND FACILITIES</span></span>
- <span data-ttu-id="fd658-192">RFC1035 ETKI ALANı ADLARı-UYGULAMA VE BELIRTIM</span><span class="sxs-lookup"><span data-stu-id="fd658-192">RFC1035 DOMAIN NAMES - IMPLEMENTATION AND SPECIFICATION</span></span>
- <span data-ttu-id="fd658-193">RFC1480 ABD etki alanı</span><span class="sxs-lookup"><span data-stu-id="fd658-193">RFC1480 The US Domain</span></span>
- <span data-ttu-id="fd658-194">RFC 2782 hizmetlerin konumunu belirtmek için bir DNS RR (DNS SRV)</span><span class="sxs-lookup"><span data-stu-id="fd658-194">RFC 2782 A DNS RR for specifying the location of services (DNS SRV)</span></span>
- <span data-ttu-id="fd658-195">RFC 3596 DNS uzantıları IP sürüm 6 ' yı destekler</span><span class="sxs-lookup"><span data-stu-id="fd658-195">RFC 3596 DNS Extensions to Support IP Version 6</span></span>